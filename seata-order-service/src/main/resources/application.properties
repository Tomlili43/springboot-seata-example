#����˿�
server.port=2001
# ������
spring.application.name=seata-order-service

# �������ã�dev��test��prod
spring.profiles.active=dev

# spring ��־����
# logging.level.root=warn
spring.main.allow-bean-definition-overriding=true

# mysql���ݿ�����
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/seata-order?serverTimezone=GMT%2B8&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=root

#����json��ȫ��ʱ���ʽ
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=GMT+8

#����mapper xml�ļ���·��
mybatis.mapper-locations=classpath:mapper/*.xml

# nacos�����ַ
spring.cloud.nacos.discovery.namespace=seata
spring.cloud.nacos.discovery.server-addr=127.0.0.1:8848

# ����openFeign��ʱʱ��
feign.client.config.default.connect-timeout=5000
feign.client.config.default.read-timeout=5000

# seata����
seata.enabled=true
seata.enable-auto-data-source-proxy=true
seata.tx-service-group=scorpios-tx-group
seata.registry.type=nacos
seata.registry.nacos.application=seata-server
seata.registry.nacos.server-addr=localhost:8848
seata.registry.nacos.username=nacos
seata.registry.nacos.password=nacos
seata.registry.nacos.namespace=seata
seata.registry.nacos.cluster=default
seata.registry.nacos.group=SEATA_GROUP

seata.config.type=nacos
seata.config.nacos.server-addr=localhost:8848
seata.config.nacos.namespace=seata
seata.config.nacos.group=SEATA_GROUP
seata.config.nacos.username=nacos
seata.config.nacos.password=nacos









